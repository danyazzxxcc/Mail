<!DOCTYPE html>
<html lang="ru">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Ошибка <%= errorCode %></title>
  <style>
  body, html {
    margin: 0; padding: 0; height: 100%;
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    color: #fff9f2;
    display: flex;

    align-items: center;
    flex-direction: column;
    min-height: 100vh;
    text-align: center;
    padding: 20px;
    user-select: none;
    background-image: url('https://cdn-kz.kursiv.media/wp-content/uploads/2025/05/img_8458-1024x576.jpeg?utm_source=in_materials');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    background-attachment: fixed;
  }

  .error-code {
    font-size: 8rem;
    font-weight: 900;
    margin: 0;
    color: rgba(255, 255, 255, 0.85);
    text-shadow: 2px 2px 8px rgba(0,0,0,0.7);
    letter-spacing: 12px;
  }

  .error-text {
    font-size: 3.5rem;
    font-weight: 700;
    margin: 10px 0 30px;
    color: rgba(255, 255, 255, 0.9);
    text-shadow: 1px 1px 6px rgba(0,0,0,0.6);
    text-transform: uppercase;
    letter-spacing: 8px;
  }

  button {
    cursor: pointer;
    padding: 14px 36px;
    border: none;
    border-radius: 8px;
    font-weight: 700;
    font-size: 1.2rem;
    background-color: #3a6ef7;
    color: #fff9f2;
    transition: background-color 0.3s, box-shadow 0.3s;
    user-select: none;
  }

  button:hover {
    background-color: #3a6ef7;
  }
</style>
</head>
<body>
  <p class="error-code"><%= errorCode %></p>
  <button onclick="window.history.back()">Назад</button>
</body>
</html>
